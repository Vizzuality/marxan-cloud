/**
 * Global prefixes for API routes
 *
 * If adding new API versions, new prefixes should be added here and used in
 * the relevant controllers.
 */
export const apiGlobalPrefixes = {
  v1: '/api/v1',
};
