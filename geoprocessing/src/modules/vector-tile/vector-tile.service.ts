// to-do: work on cache later
//import { Cache, defaultCacheOptions} from './cache';
import { createQueryForTile } from '';

//to-do: add cache options in params
export async function VectorTileServer<T>({
  maxZoomLevel = 12,
  attributes = [],
});
