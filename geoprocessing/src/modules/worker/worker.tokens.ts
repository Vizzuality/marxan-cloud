export const QueueNameToken = Symbol();
export const QueueSchedulerToken = Symbol();
export const WorkerLoggerToken = Symbol();

/**
 * path to ts file which executes the job
 */
export const WorkerExecutorToken = Symbol();
